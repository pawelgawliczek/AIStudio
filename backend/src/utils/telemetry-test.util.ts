/**
 * Telemetry testing utilities for ST-254
 */

/**
 * Simple test function to verify workflow telemetry pipeline
 */
export function logTelemetryTest(message: string): void {
  console.log(`[TelemetryTest] ${message}`);
}
